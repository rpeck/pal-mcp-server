mcp>=1.0.0,<2  # mcp 2.x removed Server.list_tools; the server + 7 upstream tests target the 1.x API. Unpinned CI pulled 2.1.1 and failed at collection.
google-genai>=1.19.0
openai>=1.55.2  # Minimum version for httpx 0.28.0 compatibility
anthropic>=0.49.0  # Native Anthropic provider; 0.49 is the floor that exposes the `thinking` arg on messages.create (the provider passes it for non-minimal thinking modes)
pydantic>=2.0.0
python-dotenv>=1.0.0
importlib-resources>=5.0.0; python_version<"3.9"

# Development dependencies (install with pip install -r requirements-dev.txt)
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
# pytest-mock>=3.11.0